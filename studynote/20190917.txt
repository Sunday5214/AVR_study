AVCC->5V 공급됨
AREF->8번핀이 AREF와 연결됨
ADMUX 00000000
00(67)->전압을 정함 01 AVCC로 5V
0(5)->Left인지 Right인지 정하는 비트
00000(01234)->몇번의 ADC 채널을 사용할지 정함
채널을 바꿀 때마다 이 값을 바꾸어야함 ex)0번은 00000인데 1번은 00001이다 두개를 번갈아쓰려면 
값을 그에 맞춰 바꿔주어야함

ADCSRA 00000000
0(7)->ADCon
0(6)->ADCstart 컨버전되는 동안은 1 끝나면 0이됨
0(5)->
0(4)->ADC 컨버전이 끝났음을 알리는 인터럽트 1이면 ISR로 아니면그냥 flag 
0(3)->인터럽트의 사용여부 enable
000(012)->ADC Prescaler

다시하는 타이머 서보시간
0.7(왼쪽)~2.3(오른쪽)
이걸 100단계하면 16us
50단계하면 32us
20ms 마다 ++
22 72